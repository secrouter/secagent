{
  "name": "secagent-pi-extension",
  "version": "0.1.0",
  "private": true,
  "description": "pi (pi.dev) extension exposing secagent affordances + use-case commands.",
  "type": "module",
  "main": "extensions/secagent.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  }
}
